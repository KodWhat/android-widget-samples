<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/xml_widget_description"
    android:minWidth="100dp"
    android:minHeight="100dp"
    android:minResizeWidth="30dp"
    android:minResizeHeight="30dp"
    android:resizeMode="vertical|horizontal"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:widgetCategory="home_screen" />

    <!--
    minWidth/minHeight corresponds to the default minimum size allowed when placing the widget.
    It can be resized below these values, following the minResizeWidth/minResizeHeight rules
    -->